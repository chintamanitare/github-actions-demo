name: Simple CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: action/checkout@v3
      
      - name: Print Message
        run: echo "Github Action is Working!"